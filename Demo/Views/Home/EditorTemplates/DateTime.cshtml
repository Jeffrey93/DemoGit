@model DateTime?

@*El operador '?' habilita al modelo tipo datetime pueda ser null: nullable, un entero no puede ser nullo, pero con el signo permite a la variable ponerle null
    Se hace para que al momento de crear un datetime se cree nulo 
    **@

@if (!Model.HasValue)
{
    <input type="date" id="@ViewData.TemplateInfo.GetFullHtmlFieldId("")" name="@ViewData.TemplateInfo.GetFullHtmlFieldId("")" value="" />
}
else
{
    <input type="date"  value="@Model.Value.ToString("yyyy-MM-dd")" id="@ViewData.TemplateInfo.GetFullHtmlFieldId("")" name="@ViewData.TemplateInfo.GetFullHtmlFieldId("")" />
}